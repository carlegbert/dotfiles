function _..() {
    if [ $# = 0 ]; then
        cd ..
        return 0;
    fi

    re='^[0-9]+$'
    if ! [[ $1 =~ $re  ]] ; then
       echo "error: Not a number" >&2; return 1;
    fi 

    for i in `seq 1 $1`; do
        cd ..
    done
    return 0;
}
